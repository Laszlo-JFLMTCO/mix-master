<%= render partial: "header" %>

<% if @header[:show_index]%>
  <p><%= link_to "Back to Artist List", artists_path, class: "btn btn-primary btn-xs" %></p>
<% end %>

<h3><span class="label label-default"><%= @artist.name %></span></h3>

<img class = "img-rounded" src = "<%= @artist.image_path %>">